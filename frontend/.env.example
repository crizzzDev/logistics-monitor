# URL base del backend.
# En desarrollo, la API local de Express:
VITE_API_URL=http://localhost:4000
# En producción (Render), reemplazar por la URL del backend desplegado, por ejemplo:
# VITE_API_URL=https://logistics-monitor-api.onrender.com
